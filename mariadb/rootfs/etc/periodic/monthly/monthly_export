#!/command/with-contenv bashio
# ==============================================================================
# Export homeassistant database periodically
# ==============================================================================

if bashio::config.true "retention.enabled" && bashio::config.equals "retention.periodic" "monthly"; then
    export-homeassistant-database
fi
